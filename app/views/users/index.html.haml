%h1 Пользователи

%table
  %thead
    %tr
      %th Имя пользователя
      %th Ник
      %th Провайдер
      %th Ссылка
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.username
        %td= user.nickname
        %td= user.provider
        %td= user.url
        %td= link_to 'Показать', user
        %td= link_to 'Редактировать', edit_user_path(user)
        %td= link_to 'Удалиь', user, :method => :delete, :data => { :confirm => 'Вы уверены?' }

%br

= link_to 'Новый пользователь', new_user_path
