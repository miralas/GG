= simple_form_for(@menu_item) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :link
    = f.input :publish
    = f.association :menu_category

  .form-actions
    = f.button :submit
