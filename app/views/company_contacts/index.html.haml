%h1 Listing company_contacts

%table
  %thead
    %tr
      %th Phone
      %th Adress
      %th Fio
      %th
      %th
      %th

  %tbody
    - @company_contacts.each do |company_contact|
      %tr
        %td= company_contact.phone
        %td= company_contact.adress
        %td= company_contact.fio
        %td= link_to 'Show', company_contact
        %td= link_to 'Edit', edit_company_contact_path(company_contact)
        %td= link_to 'Destroy', company_contact, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Company contact', new_company_contact_path
