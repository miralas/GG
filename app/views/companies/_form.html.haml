= simple_form_for(@company) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :logo
    = f.input :specialization
    = f.text_field :description

  .form-actions
    = f.button :submit
